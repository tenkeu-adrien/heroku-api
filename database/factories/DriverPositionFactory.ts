import driver_position from 'App/Models/driver_position'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(driver_position, ({ faker }) => {
  return {
    //
  }
}).build()
